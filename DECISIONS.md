# DECISIONS.md — the running ledger (SPEC §2, §12)

Format: `date — decision — why — revisitable?`

- 2026-07-02 — The constitution file is `SPEC.md`; `CLAUDE.md` is a thin pointer to it — the repo was scaffolded with `SPEC.md` at the root; SPEC §3.3 names `CLAUDE.md` as the constitution — yes (mechanical rename).
- 2026-07-02 — Packages: `SPUTTER.IMPL` holds the whole stage-0 compiler + runtime support; `SPUTTER` is the user namespace (no `:use`; mangled identifiers and prelude only) — user `fn` names must never collide with compiler internals; emitted forms reference impl symbols as objects, so nothing needs exporting across the line — yes.
- 2026-07-02 — Stage-0 scalar representations: `true` = CL `T`, `false` = `+sput-false+` singleton, `nil` = CL `NIL`; atoms, heads, and identifier names = keywords (identifier names case-sensitive, e.g. `:|total|`) — quoting a literal must yield its runtime value (I1), and CL `NIL` cannot play both `false` and `nil` (§13.2) — representation yes, semantics spec-pinned.
- 2026-07-02 — `node-equal` (and later `==` on nodes) ignores `meta` — provenance is not identity; §5.7 round-trips are "modulo meta" — yes.
- 2026-07-02 — `bin/sput` is a `/bin/sh` shim that execs `sbcl --script src/boot.lisp`; `boot.lisp` loads `~/quicklisp/setup.lisp` when present, else falls back to bare ASDF — `--script` skips init files and the frozen deps live in Quicklisp — yes (M7 saved image supersedes).
- 2026-07-02 — Two files added beyond the §3.3 layout: `src/boot.lisp` (script entry, outside the ASDF system) and `Makefile` (`make test` is the CI entry) — the launcher needs a load file and the M0 DoD wants rove green "in CI-able form" — yes.
- 2026-07-02 — Golden harness convention: `tests/golden/NAME.sput` + `NAME.expected.{expand,dump,fmt,run,show}`; `SPUTTER_GOLDEN=update` regenerates every *existing* expected file (touch an empty one to add coverage) — smallest mechanism with an explicit opt-in per mode — yes.
- 2026-07-02 — Block nodes carry `[stmt* value]`: the value slot is always the last arg; a `;`-terminated (or empty) block gets a synthesized scalar `nil` value — makes the Rust `;` rule (§5.4) structural, so pattern/splice machinery never needs a side channel; fmt canonicalizes `{ a; nil }` to `{ a; }` (same tree) — yes.
- 2026-07-02 — Statement-position `if`/`while`/`{}`/`fn` defs parse as whole statements (no operator continuation); expression position allows continuation (`let x = if c { 1 } else { 2 } + 1;` is `(if…) + 1`) — C-family reading at statement level per §12(2), uniform Pratt elsewhere — yes.
- 2026-07-02 — Paren-free condition/scrutinee positions: a bare `{` never starts an expression there (Go's composite-literal rule); the printer parenthesizes block-headed conditions — resolves `if {1} {2}` ambiguity without new syntax — no (grammar-load-bearing).
- 2026-07-02 — Assignment targets are plain identifiers only in v0.1 (`x.f = v`, `x[i] = v` are parse errors) — records are immutable (§13.8) and nothing else is assignable yet — yes (with record update syntax).
- 2026-07-02 — Float exponents require a fraction: `2e10` is malformed, write `2.0e10` — keeps `1.foo` (int + field) unambiguous and matches the spec's examples — yes.
- 2026-07-02 — Canonical layout: 100-column budget; named `fn` bodies always multiline; blocks with more than one item always multiline; single-item blocks/`if`/`while`/lambdas inline when they fit; ≥1 source blank line between statements prints as exactly 1 — derived from the §10.1 examples, locked by goldens — yes.
- 2026-07-02 — fmt canonicalizes number literals to decimal (hex/underscores not preserved; scalars carry no meta) — print contract is tree-faithful, not lexeme-faithful (§5.7) — yes.
- 2026-07-02 — `else if` chains are supported and print flat (`} else if c {`); the else slot holds a nested `.if` node — Zig has them; banning them buys nothing — no.
- 2026-07-02 — `return;` parses as `.return [nil]` (bare and `return nil;` are the same tree) — one shape for the lowerer — yes.
- 2026-07-02 — `:p.param` added as a structural child of `p.fn` (not an expression head); `arg-elem-p` admits non-keyword symbols so `p.host_call` can carry its resolved CL symbol (§6) — smallest representation that keeps everything a node — yes.
- 2026-07-02 — Operators lower to inline-declaimed rt wrappers (`sput-add` …) that type-check and panic in Sputter prose, rather than raw CL ops — a raw CL type error crossing the boundary would breach I2; SBCL still specializes through the inline declaims — yes (per-op fast paths later).
- 2026-07-02 — `/` on two integers truncates toward zero (Zig); mixed/float divides exactly; division by zero panics. `%` is *truncated* rem (CL `rem`, matching C/Zig `%` and the `.rem` head name) — the §7 table says `mod`; deviation flagged here for the human veto — yes.
- 2026-07-02 — Comparisons (`< <= > >=`) are numeric-only and return Sputter booleans (never CL NIL, which is Sputter's nil) — Zig doesn't order strings either — yes (string lib later).
- 2026-07-02 — Top-level `let`/`var` live in a global value table (`*global-values*`), never in symbol value cells — `sb-ext:global` proclamations forbid lexical shadowing (a top-level `var n` would poison every local `n`, image-wide) and specials would break closures — no (load-bearing).
- 2026-07-02 — Unknown identifiers: callee position lowers to a late-bound global call (mutual recursion works, CL-style; calling a never-defined fn renders `error: `f` is not defined`); value position is a compile-time `undefined name` error — yes.
- 2026-07-02 — Local named `fn` defs lower to `let f = nil; f = fn(...)` so the body can close over its own binding (recursion); params are immutable like `let` — Zig params are immutable — yes.
- 2026-07-02 — Prelude seed: `println` (strings raw, everything else through `show`), `show`, `panic`. The spec has no I/O primitive and `sput run` needs observable output (§12 smallest mechanism) — yes.
- 2026-07-02 — Host compile-time warnings are muffled in v0.1; §7's "SBCL derived-type warnings surfaced as Sputter warnings" is deferred (their prose is host-speak; rendering them Sputter-side needs real work) — yes (revisit at M7).
- 2026-07-02 — REPL echoes every entry's value through `show`; `let`/`fn` defs echo the bound value/function — Elixir behavior; friendlier than echoing nothing — yes.
- 2026-07-02 — Panic frames are best-effort by construction: SBCL TCO drops tail-caller frames (documented §7); `sput run --echo-last` exists as an undocumented golden-harness hook — yes.
- 2026-07-02 — (M1 review fixes) Printer prints back the `;` that pins a trailing }-terminated statement as a statement (§5.4); expressions *leading* with `if`/`while`/`switch`/`for`/`{` in statement or block-value position print parenthesized; paren-free conditions containing bare block primaries print parenthesized; a `*force-inline*` fallback renders must-break constructs inline where the grammar has no multiline form (long line > crash) — parse∘print must hold in every position, not just expression position — no (contract-load-bearing).
- 2026-07-02 — Expression/block/pattern nesting is capped at 500 levels (spanned parse error), and the §8 boundary also catches `storage-condition` — a raw "Control stack exhausted" dump breached I2 — yes (cap size).
- 2026-07-02 — Golden harness enforces an exit-code discipline: `err_*` sources must exit non-zero, everything else zero; `SPUTTER_GOLDEN=update` refuses to bake a violation into an expected file — the M1 review caught update-mode canonizing crash output — yes.
- 2026-07-02 — The parser folds negated number literals into negative scalars (`-1` *is* the literal -1) — otherwise trees carrying negative scalars (macros, patterns) can't round-trip through print — yes.
- 2026-07-02 — Identifiers are ASCII-only per §5.1; number-literal underscores must sit between digits; float overflow (`1.0e999`) is a spanned Sputter error — yes.
- 2026-07-02 — `:p.arm` joins `:p.param` as a structural Plasma head (children of `p.match`); `validate-plasma` skips arm *patterns* (§6: "p.match keeps structured patterns"), which the emitter's trivia translator owns — yes.
- 2026-07-02 — `[]` and `nil` are the same value in stage-0 (both CL NIL, spec-pinned §5.5 table): `[] == nil` is true, `[]` is falsy, `show([])` prints `nil`. Elixir distinguishes them; we can't without abandoning host lists — spec-pinned (revisit only with a repr change).
- 2026-07-02 — `for..in` desugars to a cursor `while` over `car`/`cdr` with a `#`-suffixed fresh cursor name (`#` never lexes, so capture is impossible); iterating a non-list panics; the binder is immutable — yes.
- 2026-07-02 — Switch canonical layout: always multiline, one arm per line; expression arms take trailing commas, }-terminated arms none; `else` canonicalizes to `_`; pipe chains with ≥2 stages always break, single pipes inline — derived from §10.1; goldens pin it — yes.
- 2026-07-02 — Duplicate fields in a record literal and duplicate binders in one pattern are compile errors; `.{}` (empty record) is legal — yes.
- 2026-07-02 — Prelude grew map/filter/reduce/sum/len at M3 (M7 names them, but the §10.1 DoD needs them to run); all CL-implemented, arg order list-first (Elixir) — yes.
- 2026-07-02 — Quote splicing rule (I4, uniform at term level and in macro bodies): an identifier in a quote body that names a *lexically visible* binding splices that binding's runtime value (nodes as nodes, scalars lifted to literals, anything else panics); globals — fn names and top-level lets — stay literal syntax, which is how templates mention library functions. Nested quotes stay syntax (§5.8.3 punts depth games to `insert()`) — no (macro-system-load-bearing).
- 2026-07-02 — Division of labor vs §4.3's note: the *expander* eliminates `.macro_call`/`.macro_def`/`.macro_fn_def`/`.hole`/`.splice_seq`; the *lowerer* eliminates `.quote` (and the raw/insert/inject forms inside quote bodies) — quoted code is data until spliced into a program, and splice resolution needs the lowerer's lexical environment. `raw`/`insert`/`inject` are recognized as call forms inside templates (no dedicated parser heads) — yes.
- 2026-07-02 — Head-atom bridge: internal heads are reader-cased keywords (`:ADD`); `head()`, `.head`, and `dump` downcase to the language's atoms (`.add`); `node()` upcases on construction. Same case-folding wart as identifier mangling (`.Add` ≡ `.add` as a head) — yes.
- 2026-07-02 — `meta` carries an internal `end-line` slot (parser-recorded last-token line, never exposed through `meta()`): blank-line preservation must see closing braces, which no child node spans — yes.
- 2026-07-02 — `validate-plasma` exempts `p.lit` payloads: quote lowering embeds quoted subtrees as literal *values* (a node in a p.lit is data, not IR) — yes.
- 2026-07-02 — `sput expand --dump` prints one dump per top-level form, blank-line separated; `quote(expr)` canonicalizes to bare `quote`; `meta()` returns a fresh record per call; dump renders full five-field meta records — yes.
- 2026-07-02 — `macro fn` uses parse-time signature registration to make later invocations claim balanced raw token groups; the function body is installed by the expander/evaluator through the ordinary lower→emit pipeline, and `fmt`/`expand` reset compiler state before parsing so stale in-process macro registries cannot leak across CLI calls — yes.
- 2026-07-02 — M5 hygiene is represented as internal `meta.scopes` marks plus lowerer exact-scope resolution; template binders are renamed to fresh `__hN` identifiers, while `raw` keeps a marked literal identifier and `inject` deliberately strips marks for call-site anaphora — yes.
